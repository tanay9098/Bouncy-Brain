# Railway config for the ML microservice.
# Set Root Directory = microservice in the Railway dashboard.

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
